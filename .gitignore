# Ignore npm modules directory
/node_modules

# Ignore hidden files in project directory (are from Node-Red)
/.*
# Except gitignore itself
!/.gitignore

# Node-RED Flowfile Backups
/flows/*.backup
# Node-RED local library
/lib/flows

# TW MultiWikiServer database
/public/mws/store
