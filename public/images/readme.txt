
The image files in this directory should match the image files
  in project directory ./src/nodes/icons.

The node red editor has places where it uses the URL http://localhost:1880/images
  to get images when editig properties of custom nodes.

